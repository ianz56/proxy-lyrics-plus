version: '3'
services:
  lyrics-proxy:
    build: .
    container_name: lyrics-proxy
    ports:
      - "5555:5555"
    environment:
      - PORT=5555
      - HOST=0.0.0.0
    restart: unless-stopped
